This branch is intended to really test the library with real examples without having to worry about commits, and mixing real commits with changes in the examples. This way the only thing one must do when checkouts in this branch is:

$: git rebase master
